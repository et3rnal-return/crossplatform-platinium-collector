import { PlatformHandler } from "@/classes/PlatformHandler";
import { PlatiniumTrophy } from "@/types/types";

export class PSNHandler extends PlatformHandler {
  getTrophies(): PlatiniumTrophy[] {
    return [];
  }
}
